# CodingProject1_user_feedback_form

This project is an interactive feedback form created using HTML, CSS, and JavaScript.  
It uses event listeners, DOM manipulation, event delegation, and bubbling to handle user input.